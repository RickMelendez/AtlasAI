---
phase: 01-ship-blocking-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/renderer/services/websocket.ts
autonomous: true
requirements:
  - INFRA-01

must_haves:
  truths:
    - "Frontend WebSocket connects to backend on first load (no connection refused errors in console)"
    - "Browser console shows '[WebSocket] Service initialized with URL: ws://localhost:8000/api/ws'"
    - "Connection status indicator in UI goes green within 2 seconds of page load"
  artifacts:
    - path: "frontend/src/renderer/services/websocket.ts"
      provides: "WebSocket service singleton with correct backend port"
      contains: "ws://localhost:8000/api/ws"
  key_links:
    - from: "frontend/src/renderer/services/websocket.ts"
      to: "ws://localhost:8000/api/ws"
      via: "constructor default parameter"
      pattern: "ws://localhost:8000/api/ws"
---

<objective>
Fix the one-character port typo that has prevented the frontend from ever connecting to the backend.

Purpose: The frontend has been connecting to ws://localhost:8001/api/ws while uvicorn listens on port 8000. This single bug blocks all end-to-end testing and every subsequent phase.
Output: websocket.ts with corrected port, frontend connects immediately.
</objective>

<execution_context>
@C:/Users/Rickm/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Rickm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend/src/renderer/services/websocket.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix WebSocket port 8001 to 8000</name>
  <files>frontend/src/renderer/services/websocket.ts</files>
  <action>
    On line 36 of frontend/src/renderer/services/websocket.ts, change the constructor default parameter from:
      constructor(url: string = 'ws://localhost:8001/api/ws')
    to:
      constructor(url: string = 'ws://localhost:8000/api/ws')

    Also update the JSDoc comment on lines 32-34 that documents this default URL (currently says "ws://localhost:8001/api/ws") to match the corrected port 8000.

    No other changes — this is a one-line fix. The singleton `export const wsService = new WebSocketService()` at the bottom of the file uses no custom URL, so changing the default is sufficient to fix all consumers.

    Do NOT change any reconnect logic, backoff timers, or event handler registration. The only change is the port number in the default URL string.
  </action>
  <verify>
    <automated>cd C:/Users/Rickm/Personal-Projects/AtlasAI/frontend && grep -n "localhost:8000" src/renderer/services/websocket.ts</automated>
    <manual>Start the backend (cd backend && venv/Scripts/activate && uvicorn src.main:app --reload), then start the frontend (cd frontend && npm run dev). Open browser, check console — should see "[WebSocket] Service initialized with URL: ws://localhost:8000/api/ws" and no "connection refused" errors.</manual>
  </verify>
  <done>Line 36 of websocket.ts reads 'ws://localhost:8000/api/ws'. No occurrences of 'localhost:8001' remain in the file.</done>
</task>

</tasks>

<verification>
Run: grep -rn "localhost:8001" frontend/src/
Expected: zero matches (the bug is gone)

Run: grep -n "localhost:8000" frontend/src/renderer/services/websocket.ts
Expected: at least one match on line 36
</verification>

<success_criteria>
- "localhost:8001" does not appear anywhere in frontend/src/
- "localhost:8000" appears in the constructor default on line 36 of websocket.ts
- Starting both backend and frontend results in a WebSocket connection (green indicator, no refused errors in browser console)
</success_criteria>

<output>
After completion, create `.planning/phases/01-ship-blocking-fixes/01-01-SUMMARY.md`
</output>
